packages:
  - apps/*

# Native / postinstall dependencies explicitly allowed to run build scripts.
# pnpm 11 blocks dependency build scripts by default.
allowBuilds:
  '@parcel/watcher': true
  '@prisma/client': true
  '@prisma/engines': true
  '@scarf/scarf': false # telemetry — intentionally blocked
  '@tailwindcss/oxide': true
  argon2: true
  esbuild: true
  prisma: true
  unrs-resolver: true
  vue-demi: true
