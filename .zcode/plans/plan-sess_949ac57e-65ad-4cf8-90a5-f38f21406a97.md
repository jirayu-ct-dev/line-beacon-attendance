Amend `docs/line-beacon-attendance-project-spec.md` per the scrutiny findings. No code changes — this repo is spec-only right now.

**Blocker fixes**
1. Fix hwid examples (§9, §13) to 10-hex-char values; add a "Beacon prerequisites" note (hwid issued via LINE Official Account Manager; device message must be non-zero or no event fires); add hwid format validation to §47.
2. Add explicit requirement in §7.1/§50 that the Messaging API channel and LINE Login channel must be under the same LINE provider (otherwise user IDs never match).
3. Resolve the time-authority contradiction: declare the LINE event `timestamp` the authoritative check-in time in §10/§16/§38; store server receive time separately in beacon_logs (§33).
4. Define the linking second factor in §7.1 (OTP to imported email, or admin-issued PIN); state that student code alone is insufficient.

**Major fixes**
5. §10/§36/§55: webhook flow = verify signature → persist raw event with UNIQUE webhook_event_id → 200 → process; make idempotency a hard requirement (webhookEventId + deliveryContext.isRedelivery both exist).
6. §24/§44/§45: define the expected-attendee basis and whether ABSENT is computed or materialized (recommend: computed at report time from all ACTIVE students, unless you choose a roster).
7. §4.2/§29: replace "permission" language with the flat rule — organizer edits own activities, admin edits all.
8. §35: remove/soft-define DELETE /students/:id and DELETE /beacons/:id as disable (status change, audited).
9. §20/§33: notification channel = reply token first (1-min validity, single use), push fallback; make notifications.activity_id nullable.
10. §35: add LIFF-authenticated student endpoints (GET /me, /me/attendances, /me/activities).

**Nits**
11. §12: COMPLETED derived-only. §9: complete the payload example (webhookEventId, deliveryContext, dm). §42: extend cooldown to OUTSIDE_CHECKIN_WINDOW. §48: note the NestJS global interceptor for the error envelope.

**Open choice (will ask before applying):** whether to apply the MVP scope cuts — trim student LIFF pages to Link+Profile (§21/§52/§61) and drop packages/shared+config from the repo structure (§59) — or keep the full scope and only fix correctness issues.